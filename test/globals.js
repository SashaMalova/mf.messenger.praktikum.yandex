global.window = {
    location: {
        pathname: '/name',
    },
};

global.document = {
    createElement: () => {
    },
    querySelector: () => {
    },
};

global.Handlebars = {
    compile: () => {
        return () => {
        }
    },
    registerPartial: () => {},
};

